# This is not a configuration file needed by XMonad.
# I source it from .xinitrc. It contains all the commands needed
# to start my XMonad.

# set cursor
xsetroot -cursor_name left_ptr

# compositing manager for transparency
#compton -c -o.55 -r8 &
#compton -bcCGf -i 0.95 -e 0.95 -o 0.55 &
compton -bcCG -i 0.95 -e 0.95 -o 0.55 &

# set wallpaper
#hsetroot -center ~/Pictures/wallpaper &
sh ~/.fehbg &

# start window manager
exec xmonad

